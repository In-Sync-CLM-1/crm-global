name = "globalcrm-cron-dialer"
main = "src/index.js"
compatibility_date = "2026-05-01"
vars = { TARGET_FN = "ai-bulk-call" }

# Dialer tick — queues + dispatches AI calls within each org's window.
[triggers]
crons = ["* * * * *"]
